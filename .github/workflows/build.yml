on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'mybranch1'
    paths:
      src/**.py
      
jobs:
  greet:
    runs-on: [ubuntu-latest]
    steps:
      - name: say hi
        run: |
          echo "Hello World2"
          pwd
          python ../src/hello.py
   
